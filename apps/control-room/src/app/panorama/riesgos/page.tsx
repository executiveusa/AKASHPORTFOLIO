"use client";

export default function RiesgosPage() {
  return (
    <div style={{ minHeight: "100vh", background: "var(--color-bg)", color: "var(--color-text)", fontFamily: "var(--font-sans)", padding: 16 }}>
      <h1 style={{ fontFamily: "var(--font-display)", fontSize: 20, fontWeight: 700, marginBottom: 4 }}>Registro de Riesgos</h1>
      <p style={{ color: "var(--color-muted)", fontSize: 13 }}>Risk register PMBOK — próximamente.</p>
    </div>
  );
}
