# Wiring audit (lite) — for 01_discovery
Laws: presence ≠ completion · one boss per truth · trace promise → surface → handler → transport → capability → dependency → canonical state → observable result → evidence · test the failure path · runtime truth outranks repo intent.
Per surface record: route, control, handler, transport, backend, dependency, state owner, states (loading/empty/error/success), evidence. Classify: WIRED / PARTIAL / MOCK / STUB / DEAD / UNSAFE. Flag STATE_FRAGMENTATION where two stores mutate one truth.
Full method: owner's Full-Stack Wiring Audit v2 (loaded only when a run's scope is the whole app).
