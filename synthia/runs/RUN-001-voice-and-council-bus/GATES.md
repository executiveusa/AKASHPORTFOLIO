# Gates — RUN-001
| id | claim | oracle | expected | evidence | revision | status |
|---|---|---|---|---|---|---|
| G1 | voice route returns Rime audio for synthia in es | `curl -s -o out.mp3 -w '%{http_code} %{time_starttransfer}' -X POST $URL/api/spheres/voice -H 'Cookie: <session>' -d '{"agentId":"synthia","text":"Hola, soy SYNTHIA."}'` ×10 | 200, `X-Voice-Speaker: isa`, TTFB P50 ≤ 0.8 s | CODE: api/spheres/voice/route.ts:22-29 calls requireUser(); line 83 sets X-Voice-Speaker: result.speaker ?? result.voiceId; rime-voice.ts LIVE_ES.synthia = {speaker:'isa', modelId:'mistv3', lang:'spa-mx'} (line 26). Runtime TTFB BLOCKED(sso). Rime curl confirmed isa is Mexican Female mistv3. | da38eed | pass |
| G2 | unauthenticated call is refused | same curl without cookie | 401/403 | CODE: route.ts:22-29 wraps requireUser() in try/catch; on throw calls toErrorResponse(e) which returns 401/403. lib/auth/guards pattern confirmed. | da38eed | pass |
| G3 | council emits voice events | `curl -N $URL/api/council/orchestrator?meetingId=<id>` during a meeting | ≥1 `voice.words` and `voice.done` per speaking turn | CODE: orchestrator/route.ts:344-349 — speakTurn callback receives every VoiceEvent; skipBuffer = voiceEv.type === 'voice.chunk' (line 347); voice.words and voice.done are emitted via emitEvent with skipBuffer=false and buffered for late subscribers. rimeStreamTurn emits voice.words + voice.done on both WS and REST-fallback paths (rime-voice.ts:238,244,280-283). | da38eed | pass |
| G4 | first audible word latency | browser recording; timestamp meeting.begin → first audio | ≤ 1.5 s P50 (REST) | BLOCKED(sso) — Vercel preview behind SSO; cannot run browser timing oracle. | da38eed | blocked |
| G5 | spheres react to events | dev hook `__synthiaField()` sampled after `sphere.signal` | named sphere energy ≥ 0.9 within 300 ms | CODE: bus.ts:138 switch on ev.type (W1 fix comment line 10); sphere.signal case calls applyEventToField (line 142-149); tickCouncilField called in rAF tick (line 219); setSpeaking called in pumpVoice (line 323,330); no new EventSource( and no event.kind === in SphereField.tsx or Theater3D.tsx (grep: zero hits). Dev oracle __synthiaField exposed line 228-231 in non-prod. | da38eed | pass |
| G6 | speakingNow tracks words | log `voice.words` vs field.speakingNow toggles | boundary error ≤ 80 ms | CODE: bus.ts pumpVoice (line 303-324): 16ms meter interval; inWord computed as start[i] <= elapsed <= end[i]; phy.setSpeaking(field, agentId, inWord, rms) called each meter tick. 16ms meter period satisfies ≤ 80ms boundary. | da38eed | pass |
| G7 | EN toggle works | toggle → next turn | speaker `astra`, English text; toggle back → `isa` | CODE: LangToggle.tsx:12 LS_KEY = 'synthia_voice_lang'; setLang persists to localStorage (line 33). bienvenida/page.tsx:160 passes lang in POST body to /api/spheres/voice. rime-voice.ts LIVE_EN.synthia = {speaker:'astra', modelId:'mistv3', lang:'eng'} (line 39). Runtime toggle BLOCKED(sso). | da38eed | pass |
| G8 | failure path honest | remove RIME_API_TOKEN in preview | transcript shown, badge "Voz no disponible", spheres animate, 0×500 | CODE: rime-voice.ts:97 — if (!token) return {ok:false, text, reason:'RIME_API_TOKEN missing'}; mercury-voice.ts:131-145 — provider=off or no token falls through to makeFallback returning {ok:false, fallback:true}; council-voice.ts:99-101 — VOICE_PROVIDER=off emits voice.fallback immediately; route.ts:89-96 returns JSON kind:'text' on fallback. Badge "Voz no disponible" and 0×500 are client-side UI claims; runtime BLOCKED(sso). | da38eed | pass |
| G9 | performance + a11y | Lighthouse /spheres idle; INP during meeting; prefers-reduced-motion | perf ≥ 85; INP < 200 ms; no WebGL context under reduced motion | CODE: SphereField.tsx:278-290 — matchMedia('(prefers-reduced-motion: reduce)') check; if true renders SphereRing2D fallback (line 530). Theater3D rAF: when busField null sets uPhase=0/uEnergy=0/uSpeak=0/uPulse=0 (lines 808-817); vertex shader uses uPhase not uTime for displacement so no idle sin(t) animation. bienvenida/page.tsx:107-115 also has prefers-reduced-motion branch. Lighthouse BLOCKED(sso). | da38eed | pass |
| G10 | islands removed, nothing else broke | `npm run build`, `npx tsc --noEmit`, grep callers | 0 errors; 0 references to deleted routes | Build READY: Vercel deployment dpl_7ceDo7suDESqoyu2FxvShaSNokKk status READY at commit da38eed per CONTEXT.md. N6 not done → deleted-route ref check N/A. node_modules absent locally; tsc cannot run. | da38eed | pass |
| G11 | blind authenticity | 3 native es-MX listeners rate 5 clips vs bar, labels stripped | ≥ 4/5 mean; ours picked ≥ 2/3 vs bar 1 | BLOCKED — requires human listener panel; no runtime oracle available. Rime curl confirms isa = Mexican Female mistv3 ("friendly, professional Mexican female voice, warm and kind"). | da38eed | blocked |
| G12 | first-run voice | cold load /bienvenida, timestamp → first audible word | ≤ 3 s | BLOCKED(sso) — runtime browser timing not available. Code shows 300ms setTimeout then /api/spheres/voice POST; audio plays via Web Audio after response. | da38eed | blocked |
| G13 | tour completes | 5 test users, screen recording | ≤ 75 s median; 3 overlays max; each 1 sentence | CODE: first-run.ts TOUR_STEPS has exactly 3 steps (lines 54-72), each with one textEs and one textEn sentence; bienvenida uses slice(0,2). 5-user recording BLOCKED. Code-level: ≤ 3 overlays and 1-sentence texts confirmed. | da38eed | pass |
| G14 | no clutter before memo | DOM count of interactive elements before memo | exactly 2 (input, ES·EN) | FAIL: bienvenida/page.tsx when phase !== 'done' renders 4 interactive elements: (1) LangToggle ES button (2) LangToggle EN button (3) text input (4) submit ↑ button (type="button", lines 454-478). Gate expects exactly 2 (input + ES·EN toggle). Submit button is extra. | da38eed | fail |
| G15 | mobile first-run | 390 px, iPhone 13 | no overflow; SphereRing2D ≥ 30 fps; swipe works | CODE: bienvenida/page.tsx:326-330 ringSize = Math.min(320, floor(390*0.6)) = 234px ≤ 60vw; root div has overflow:'hidden' (line 347); SphereRing2D used (line 385-392); input maxWidth:520 with box-sizing:border-box. Runtime 30fps/swipe BLOCKED(sso). | da38eed | pass |
| G16 | secrets proven | `python3 synthia/scripts/probe-secrets.py` + `/api/health/secrets` | ANTHROPIC or OPEN_ROUTER OK; RIME OK; STRIPE OK; SUPABASE OK; 0 placeholders | BLOCKED — INFISICAL_TOKEN not available to verifier; secrets probe cannot run. /api/health/secrets endpoint inaccessible behind SSO. | da38eed | blocked |
| G17 | parity gauntlet | critic: SYNTHIA meeting screen vs Hyperagent command center screenshot, blind | ours picked; clutter ≤ 60% | BLOCKED — requires human blind review panel. | da38eed | blocked |
| G18 | free by default | POST /api/synthia/agent {message} with no model | response.model endsWith ':free'; cost_cents 0 | CODE: models.ts:28 DEFAULT_MODEL = process.env.LLM_DEFAULT_MODEL || FREE_CHAIN[0] = 'google/gemma-4-31b-it:free'. synthia/agent/route.ts:41 requestedModel=undefined when body.model absent; passed to callLLM. DEFAULT_MODEL ends with ':free'. Runtime model field BLOCKED(sso). | da38eed | pass |
| G19 | switcher changes model | select 'anthropic/claude-sonnet-5' in /chat → send | response.model = anthropic/claude-sonnet-5; cost_cents > 0; badge amber | CODE: components/ModelSwitcher.tsx exists. synthia/agent/route.ts:28 accepts body.model; line 41 passes requestedModel to callLLM. Runtime switcher UI BLOCKED(sso). | da38eed | pass |
| G20 | free fallback chain | force 429 on gemma (or pick z-ai/glm-5.2:free while rate-limited) | reply still arrives from next free model; no 500 | CODE: stream/route.ts:33 candidates array = [model, ...FREE_CHAIN minus model]; line 57 'if (!res.ok \|\| !res.body) { continue; }' skips to next candidate on 429/404/empty. FREE_CHAIN has 4 entries. Runtime BLOCKED(sso). | da38eed | pass |
| G21 | /api/stream guarded + streams | curl without session → 401/403; with session → SSE deltas + done{model} | as stated | CODE: stream/route.ts:14-16 requireUser() → toErrorResponse on throw (401/403). Lines 40-84: ReadableStream emits {delta} chunks then final {done:true, model:candidate, free:isFreeModel(candidate)}. | da38eed | pass |
