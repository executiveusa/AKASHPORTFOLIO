# Gauntlet — RUN-001 — pass 1 — 2026-09-05 (critic: fresh opus context; judged at local 6b58f85 = remote da38eed)
Blind pick: BLOCKED (no A/B artifacts; preview SSO). Parity: SAME 2 · BETTER 3 · WORSE 6 · MISSING 5. Clutter: 4 vs ~21 (19%) — hollow pass (absence ≠ subtraction).
Collins 5.9/10 — all floors FAIL. Voice 31/100 (TTFA not designed for: client waits for voice.done; pronunciation 0/10; fallback plumbed but never rendered).
Critical failures (5): first-run unreachable (`/`→/dashboard, no middleware) · orchestrator admin-only on the welcome path · canned static memo styled as council output · `/api/council/memo` referenced but missing · approvals inert (count only, no control, no reason shown).
Unverified claims (4): tour "pulso financiero en tiempo real" over a hardcoded 'MX' · "PRESUPUESTO" is a physics scalar · "accessible via click" false · STATE revision hash mismatch (local vs remote sha — fixed: record both).
Load-bearing: in-memory SSE pub/sub across Vercel lambdas (POST/GET may land on different instances) · HUD contrast 9px/0.35 · AudioContext without gesture · "Siguiente" button · unbounded camera random walk · runtime @import font · two 500+ line renderers.
Single biggest gap: the 60-second first run cannot happen.
Repair slice 1 (dispatched 14:40): `/` cookie redirect · bienvenida path = requireUser (not anon) + 3 spheres + caps · lang threaded · self-identifying sample memo + voice fallback badges · AudioContext on gesture · no violet accent · TourOverlay no "Siguiente" · SphereField HUD 12px/.62, ENERGÍA, hide market w/o data, damped shake, idle rAF stop, raycaster + sr-only sphere buttons · Theater3D idle spin/particles removed · fleet h1 es · /api/council/memo route.
Next slices (critic order): TTFA — play first chunk progressively (bus pump on first chunk, not voice.done) · approvals control on /spheres (reason + approve/reject → /api/approvals) · memo → /api/synthia/assets + /library · SSE durability (Supabase Realtime or KV) for multi-instance Vercel.
Verdict: NOT READY

# Gauntlet — RUN-001 — pass 2 — 2026-09-05 (fresh opus critic; local 9f68361 = remote 3dc3dbf; Vercel READY dpl_8jhwtUWWWphP6EkXCu9b9PYUkqbC)
Parity unchanged (SAME 1 · BETTER 2 · WORSE 6 · MISSING 5) — "the repair moved honesty, not capability". Collins 6.0 (floors fail). Voice 40/100 (honesty 9/10 now; TTFA still 5/25 at that revision — slice 2 addresses).
Pass-1 items: FIXED C3 sample-memo badge, C4 memo route (thin), U1 tour copy, U2 ENERGÍA, U3 click/keyboard, U4 sha, L2 contrast, L4 Siguiente, L5 SphereField shake. PARTIAL C2 (requireUser but first-time visitor has no session), L3, Theater3D idle, Spanish labels. NOT FIXED C1 — `next.config.ts` redirects `/`→`/landing` so app/page.tsx is dead code. NOT FIXED C5 — nothing emits approval.required (dead PENDIENTES readout). NOT FIXED L1 in-memory pub/sub across lambdas (now also gates GET auth). REGRESSION L6 — fonts don't ship (no next/font, no @font-face). WORSE L7 — SphereField 696 lines.
Single biggest gap: entry point still wrong (config redirect) + no session for first-time visitor + fonts.
Owner decision recorded: welcome council stays requireUser (signed-in operator), NOT anonymous — `/` routes: no session → /landing; session + no cookie → /bienvenida; else /dashboard.
Repair slice 2 (Tempo, done): MediaSource progressive playback (TTFA), ApprovalCard + resolveApproval, memo → synthia_assets row (type 'memo').
Repair slice 3 (Keystone, dispatched): remove config redirect + session-aware `/`; HMAC meeting token for SSE across instances; approval.required producer (confidence < 0.85 or spend verbs) + approvals row; next/font Plex + var() font families; Theater3D rAF gate + coherence-bound drift; neutral council ring; fleet Spanish labels; unlockAudio on first gesture.
Verdict: NOT READY

# Gauntlet — RUN-001 — pass 3 — 2026-09-05 (fresh opus critic; local 25943c5 = remote 48c174d; build UNVERIFIED — Vercel quota)
Parity SAME 1 · BETTER 0 · WORSE 7 · MISSING 4. Collins 5.5. Voice 37/100.
Criticals (4): approval.required emitted AFTER controller.close() on meeting.end → ApprovalCard can never render live · /bienvenida bypasses the CouncilBus (own EventSource/AudioContext) → MediaSource TTFA dead on the first-run screen · no watchdog → first run can hang forever if POST/GET land on different lambdas · in-memory pub/sub across instances still unfixed (HMAC token = auth only, not transport).
New defects N1–N10: approval gate ~100% false positives (bare enviar/publicar; coherence ∈ {0,.33,.67,1} with 3 spheres) · `\$` inside \b never matches · spend approvals risk 'high' → non-admin 403 · tour one-step dead end · LangToggle inert (per-component state) · MSE appendBuffer throw deadlocks queue · HMAC 'dev' fallback, token in query · bus EventSource never sends token · useSpeaker() new-object selector loop risk · three token systems + "Cynthia"/"SYNTHIA" split.
Likely type errors: Uint8Array<ArrayBufferLike> vs <ArrayBuffer> in bus.ts; unused vars; next/font/google build fetch (CI note in next.config); globals.css --font-sans → undefined vars; literal "IBM Plex Mono" in spheres/page.tsx.
Repair slice 4 (Mender-II, dispatched 15:35): approval before close + 750 ms close delay · gate sanity (real spend phrases, ≥3 votes, bienvenida never gates, risk medium unless spend) · bienvenida on the bus (connect(meetingId,{token}), ApprovalCard, watchdog 20 s / 90 s cap) · lang single source (bus + localStorage hydrate) · tour progress-driven, skip missing anchors · type prefix (Uint8Array<ArrayBuffer>, unused vars, MSE deadlock, useSpeaker, HMAC secret required, bus sends token) · fonts (body/globals → Plex vars) · landing CTAs → callbackUrl=/ · memo route falls back to Supabase sphere_meetings · camera shake base+offset.
Verdict: NOT READY
