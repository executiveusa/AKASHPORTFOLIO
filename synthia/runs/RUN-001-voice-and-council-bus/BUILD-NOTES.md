# Build notes — RUN-001 (commit 8a9c2cb, 2026-09-05)
Builders: Vox (N0+N1), Pulse (N3), Aurora (N4+N8), Relay (N2), Prism (N5), Ledger (N7). Orchestrator: this thread. N-1 (models) earlier.

Changed: .env.example · package.json (+zustand) · api/council/orchestrator (voice events, registry calls) · api/spheres/status (real) · api/spheres/voice (+guard, Rime) · api/spheres/voice/catalog (new) · app/bienvenida (new) · cockpit/fleet (real) · cockpit/salon (bus) · app/spheres (subtracted) · components: LangToggle, SphereField (rewrite), SphereRing2D, Theater3D (bus), tour/* · lib/council/{bus,registry,selectors,README} · lib/first-run · lib/mercury-voice (Rime-first) · lib/sphere-physics (setSpeaking) · lib/voice/{rime-voice,council-voice} · shared/council-events (+voice.*, approval.required).

Unsure / risks: zustand not in lockfile (Vercel npm install must resolve); `import('ws')` resolves transitively (lockfile has ws ^8.18); `lang` not threaded into council-engine LLM text (voice only); voice queue strictly sequential; SphereField 544 lines (>500 guideline); /bienvenida requires an authenticated admin for the live welcome council (degrades to static memo otherwise); registry in-memory (cold-start resets).
Env needed (names): RIME_API_TOKEN, VOICE_PROVIDER, VOICE_MEETING_CHAR_CAP, VOICE_DAILY_CHAR_CAP, OPEN_ROUTER_API, LLM_DEFAULT_MODEL, LLM_ALLOW_PAID.

## Repair slice 1 (Mender, commit 9f68361 / remote 3dc3dbf)
`/` server component: cookie synthia_seen → /bienvenida else /dashboard · orchestrator: initiatedBy 'bienvenida' → requireUser (signed-in operator, NOT anonymous), agentIds ⊆ {synthia, alex, cazadora}, GET SSE relaxed for those meetingIds (module Set, 2 h TTL) · bienvenida: lang in POST, sample memo self-identifying (dashed, "Sin consejo en vivo — memo de ejemplo"), "Voz no disponible" badge, AudioContext created/resumed on first gesture, runtime @import removed, neutral accent · TourOverlay: no "Siguiente" · SphereField: FALLBACK transcript style + badge, HUD 12px/.62, ENERGÍA, market hidden without data, damped shake, rAF stops when field null, raycaster click + sr-only sphere buttons · Theater3D: idle spin + particle drift removed, damped shake · fleet h1 es · new GET /api/council/memo · SpheresTour step 3 copy honest.
Not yet: TTFA (bus plays only after voice.done), approvals control on /spheres, memo → /api/synthia/assets, SSE durability across Vercel instances, pronunciation registry, N6 deletions (owner gate).

## Repair slices 2+3 (Tempo + Keystone, commit 7f47921 / remote 738d598)
Slice 2: bus.ts progressive playback via MediaSource (audio/mpeg SourceBuffer per clip; play on first chunk; Safari fallback decode-after-done; metrics.ttfaMs + meetingToAudioMs on __synthiaField) · ApprovalCard (Aprobar/Rechazar → POST /api/approvals/[id]/decision; A/R keys; Esc) mounted in SphereField with data-tour aprobaciones · orchestrator writes memo row to synthia_assets (type 'memo', url /api/council/memo?briefId) on meeting.end.
Slice 3: next.config redirect `/`→`/landing` removed; `/` = auth() ? (cookie ? /dashboard : /bienvenida) : /landing; signin default callbackUrl '/' · HMAC(meetingId) token returned by bienvenida POST and accepted on GET SSE (cross-instance) · approval.required PRODUCER: confidence < 0.85 or spend verbs → approvals row + SSE event + La Vigilante ASSERT signal · next/font IBM Plex Sans/Mono variables on <html>; all new components use var(--font-plex-*) · Theater3D rAF stop/start gate, particle drift × groupCoherence · council ring neutral 0xe8e9ee · fleet labels Spanish · unlockAudio() on first gesture; bus guards AudioContext behind it and re-pumps.
Unverified: build of 738d598 (Vercel API quota hit). Risks: MediaSource path untested in browser; approvals table columns assumed from route code; synthia_assets scope filter absent in GET route.

## Repair slice 4 (Mender-II, commit 50a79a3)
A approval.required + La Vigilante ASSERT emitted before meeting.end; SSE close delayed 750 ms · B spend pattern narrowed to real spend phrases; confidence gate uses memo.confidence else coherence only with ≥3 votes else skip; bienvenida never gates; risk high only on spend · C /bienvenida uses bus.connect(meetingId,{token}); ApprovalCard rendered; greeting clip kept · D watchdog 20 s rolling + 90 s cap → dashed sample memo · E lang single source: bus + localStorage hydrate · F tour advances by progress; mercados step skipped when anchor absent · G Uint8Array(new ArrayBuffer), unused vars removed, MSE appendBuffer failure → cleanup, useSpeaker primitives, HMAC secret required (500 otherwise), bus SSE sends token · H body/globals fonts → Plex vars; "Cynthia"→"SYNTHIA" · I landing CTAs callbackUrl=/ · J memo route → Supabase sphere_meetings fallback; registry insert id/closed_at · K camera shake base+offset.
Unverified: build (quota). Next known gaps (critic pass 3): in-memory SSE pub/sub across lambdas (needs Supabase Realtime/KV), pronunciation registry, SphereField >500 lines, cockpit token systems, N6 deletions (owner gate).

## 2026-09-10 — build green
Two typed-array widening errors fixed in src/lib/council/bus.ts (pendingBytes + b64ToUint8 → Uint8Array<ArrayBuffer>; analyser buffer; decodeAudioData path). Commits 9b556b2, e756a0e (remote 55efdd4 → e756a0e). Vercel dpl_ETL84DVoUSJtfHtoiMchfZL46keq **READY** — whole RUN-001 slice now compiles. Preview is SSO-protected; runtime gates remain pending owner access.
